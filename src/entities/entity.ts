abstract class Entity {
  position: { x: number; y: number } = { x: 0, y: 0 };

  init() {
    //
  }

  update(dt: number) {
    //
  }

  fixedUpdate(dt: number) {
    //
  }

  render() {
    //
  }
}

export default Entity;
